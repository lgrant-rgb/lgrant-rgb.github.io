# lgrant-rgb.github.io
a browser where you can use key combos to change the page and help would be appriciated, it uses uframe but ive linked it to bing which has no xframe so the host browser decides iframe pref
